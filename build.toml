[general]
name = "yarqa-attn"
version = 1
edition = 5
license = "Apache-2.0"
backends = ["cpu"]
source = "https://github.com/szl-holdings/YARQA-ATTN"

[general.hub]
repo-id = "SZLHOLDINGS/YARQA-ATTN"

# CPU torch-noarch. No AOT .cu / torch_binding. GPU cubins are not claimed.
# Do not set kernel.backend = "triton" (obsolete; that is the trio, not this cut).
[torch-noarch]
pyext = ["py"]
